import { connect } from 'react-redux';
import IndexBody from './index_body';

const mSTP = (state, ownProps) => ({
  
})

const mDTP = (dispatch) => ({

})

export default connect(mSTP, mDTP)(IndexBody)